import socketcluster from "socketcluster-server";
import handleRequest from "../../core/macroRoutines/RequestHandling";

export async function login(socket:socketcluster.AGServerSocket):Promise<void>
{
    
}

export async function tedRequest(socket:socketcluster.AGServerSocket):Promise<void>
{
    console.log("test");
        
}